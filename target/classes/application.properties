spring.application.name=study-manager

# Active profile
spring.profiles.active=dev

# Server no context-path so PWA static files are served from /
# API controllers are prefixed with /api in their @RequestMapping
server.port=8080
